网络拓扑示意图客户端IP：10.10.11.12被阻断服务器IP：10.10.10.22阻断口IP：10.10.11.20ssh登录xdr服务器，查看阻断口的网卡名称和对应的网关MAC地址下图为本次测试中阻断口对应的配置输入以下命令查看阻断口对应的网关MAC地址arp -n结果如下图所示，阻断口的网卡名称对应的HWaddress就是对应的网关MAC地址在xdr服务器上执行以下命令进入apt容器kubectl exec -it `kubectl get pod -n ailpha-xdr | grep apt | awk -F ' ' '{print $1}'` -n ailpha-xdr /bin/bash 配置MAC地址方式特别说明：确保阻断口和服务器客户端联通进入apt容器后登录mysql，并执行以下命令在数据库中更新阻断口的相关配置注意：联系产线远程协助登录mysqlenp125s0f1为步骤1中阻断口网卡的名称；70:3a:a6:17:30:f6为步骤1中阻断口对应的网关MAC地址update wdd_sysconfig set val='enp125s0f1' where item = 'gsc_block_nic';update wdd_sysconfig set val='70:3a:a6:17:30:f6' where item = 'gsc_block_mac';update wdd_sysconfig  set val='1' where item = 'gsc_send_packet_mode';exit到/data/vpp/vppids/config目录下，执行以下命令更改start.conf -&gt;start_high.confmv start.conf start_high.conf另外编辑文件start.setup，按下图红框所示修内容（注意：enp125s0f1为步骤1中业务口网卡的名称，num-tx-queues后面的数字由0改为3）全部修改后执行以下命令重启apt业务ssr客户端上用浏览器访问http://10.10.10.22/，web界面正常。登录apt平台，进入【配置/联动阻断/旁路阻断】页面，新增策略，内容如下图：策略新增成功后需要手动启用策略，弹框中点击&lt;确定&gt;即可。策略内容按现场实际情况填写。客户端上用浏览器访问http://10.10.10.22/，web界面无法正常访问。不配置MAC地址方式特别说明：确保阻断口是trunk模式，输出流量带上vlan标签进入apt容器后登录mysql，并执行以下命令在数据库中删除阻断口对应的网关MAC地址注意：联系产线远程协助登录mysqlupdate wdd_sysconfig set val='' where item = 'gsc_block_mac';exit执行以下命令重启apt业务ssr禁用步骤4.4中新增的策略，客户端上用浏览器访问http://10.10.10.22/，web界面正常。启用步骤4.4中新增的策略，客户端上用浏览器访问http://10.10.10.22/，web界面无法正常访问。