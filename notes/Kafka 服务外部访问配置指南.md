---
title: Kafka 服务外部访问配置指南
tags: []
created: 2026-05-08
type: permanent
summary: 
---


# Kafka 服务外部访问配置指南


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


### Kafka 服务外部访问配置指南


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 文档概述


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

本文档旨在指导运维或开发人员，如何配置以便从 Kubernetes 集群外部访问内部部署的 Kafka 服务。核心原理是通过 Kubernetes 的 NodePort 类型 Service，将 Kafka Broker 的端口映射到集群节点的指定端口上，从而实现外部访问。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 1. 平台侧检查（无需配置）


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

平台侧（Kubernetes 集群内）的 Kafka 服务已经预先配置妥当。运维人员只需确认相关的 NodePort Service 存在且状态正常即可。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 2. 外部发送端前置要求


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

外部客户端（生产者/消费者）需要能够解析 Kafka Broker 的内部域名。这通常需要通过配置客户端的 hosts 文件来实现。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 3. 获取连接信息


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

连接信息包括内部域名和外部端口。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

3.1 查看 Kafka 服务清单

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

在 Kubernetes 集群的 Master 节点上执行以下命令，查看所有 Kafka 服务：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

kubectl get svc -n kafka

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

您将看到类似下面的输出：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

NAME                      TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)                                        AGE
kafka-0-ecdn              NodePort    172.25.254.223   <none>        9093:39091/TCP                                 5h21m
kafka-0-epdn              NodePort    172.25.254.184   <none>        9095:19191/TCP                                 5h21m
kafka-0-epip              NodePort    172.25.254.19    <none>        9094:19091/TCP                                 5h21m
kafka-cruisecontrol-svc   ClusterIP   172.25.254.249   <none>        8090/TCP,9020/TCP                              5h20m
kafka-exporter            ClusterIP   172.25.254.170   <none>        9308/TCP                                       5h21m
kafka-headless            ClusterIP   None             <none>        9092/TCP,9094/TCP,9095/TCP,9093/TCP,9020/TCP   5h21m

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

3.2 识别关键服务与端口

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

在上述输出中，请关注以下两种类型的服务，它们分别用于不同场景的访问：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

名词解释：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 4. 外部客户端配置步骤


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

4.1 配置 Hosts 解析

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

由于外部网络无法直接解析 Kubernetes 的内部域名，必须在外部客户端机器的 hosts 文件（如 /etc/hosts 或 C:\Windows\System32\drivers\etc\hosts）中，手动添加域名映射。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

映射规则：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

{集群节点的公网IP}   {Kafka Service 内部域名}

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

配置示例：
假设您有一个 Kafka Broker (kafka-0-epdn)，并且您的 Kubernetes 节点公网 IP 是 192.0.2.100。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

# 在外部客户端的 hosts 文件中添加
192.0.2.100   kafka-0-epdn.kafka.kafka.svc.cluster.local:19191

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

4.2 配置 Kafka 客户端连接地址

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

在外部客户端的 Kafka 配置文件中，使用以下格式配置 bootstrap.servers：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

{内部域名}:{外部端口}

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

配置示例（接上例）：

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

bootstrap.servers = kafka-0-epdn.kafka.kafka.svc.cluster.local:19191

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 5. 内部客户端配置步骤


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

对于集群内部或其他可直接访问 Kubernetes 节点内网 IP 的设备，配置相对简单。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

在客户端的 Kafka 配置中，直接使用 Kubernetes 节点的内网 IP 和对应的外部端口。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

配置示例：
假设 Kafka 节点内网 IP 为 10.0.1.20，使用 epip 服务。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

bootstrap.servers = 10.0.1.20:19091

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 6. 多 Kafka Broker 配置


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

当集群中有多个 Kafka Broker（例如 kafka-0, kafka-1, kafka-2）时，每个 Broker 都会有对应的 epdn 和 epip 服务。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

您需要为每一个 Broker 重复 第4步 中的配置。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

获取所有 Broker 的服务名和端口（参考 3.1 和 3.2）。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

在外部客户端 hosts 文件中，为每个 Broker 的域名添加一条记录，它们可以全部指向同一个公网 IP。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

在 Kafka 客户端配置中，将所有 Broker 的地址用逗号分隔。注意：每个 Broker 的 NodePort 可能不同，请根据 kubectl get svc 的实际结果填写。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)


#### 7. 注意事项


服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

网络连通性：确保外部客户端能够访问 Kubernetes 集群节点的公网 IP 和指定的 NodePort（如 19191）。检查防火墙和安全组规则是否放行。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

高可用：在生产环境中，建议配置多个 Broker 的地址以实现高可用。客户端只需连接上一个即可获取完整的集群元数据。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

端口差异：不同 Broker 的 NodePort 可能不相同，配置时请仔细核对。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

Advertised Listeners：此配置基于 Kafka 的 advertised.listeners 正确设置。如果遇到连接问题，请确认 Kafka 本身的监听器配置。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

修订记录

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

优化了文档结构和语言，使其更清晰易懂。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

增加了概述、术语解释和配置示例。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

明确了内外网配置的区别。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

详细说明了多 Broker 的配置方法。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)

增加了注意事项。

服务类型 | 命名规则 | 用途 | 外部端口示例

外部访问服务 | kafka-<序号>-epdn | 供集群外部客户端使用。 | 19191 (对应 Service 端口 9095)

内部访问服务 | kafka-<序号>-epip | 供集群内部或其他特定网络环境的客户端使用。 | 19091 (对应 Service 端口 9094)
